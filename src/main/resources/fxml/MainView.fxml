<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="it.uni.biblioteca.controller.MainController"
            fx:id="mainBorderPane"
            prefHeight="800.0" prefWidth="1200.0"
            styleClass="root">
    
    <!-- LEFT: Sidebar Navigation (invece del menu bar) -->
    <left>
        <VBox styleClass="sidebar" spacing="5.0" prefWidth="200.0">
            <padding>
                <Insets top="20.0" right="10.0" bottom="20.0" left="10.0"/>
            </padding>
            
            <!-- Logo/Titolo -->
            <VBox spacing="5.0" alignment="CENTER" styleClass="sidebar-header">
                <padding>
                    <Insets bottom="20.0"/>
                </padding>
                <Label text="📚" style="-fx-font-size: 36px;"/>
                <Label text="Biblioteca" styleClass="sidebar-title"/>
                <Label text="Universitaria" styleClass="sidebar-subtitle"/>
            </VBox>
            
            <Separator/>
            
            <!-- Navigation Buttons -->
            <Label text="NAVIGAZIONE" styleClass="sidebar-section-label">
                <padding>
                    <Insets top="15.0" bottom="5.0" left="5.0"/>
                </padding>
            </Label>
            
            <Button fx:id="btnHome" text="🏠 Home" 
                    onAction="#apriHome"
                    styleClass="nav-button"
                    maxWidth="Infinity"/>
            
            <Button fx:id="btnLibri" text="📖 Gestione Libri" 
                    onAction="#apriGestioneLibri"
                    styleClass="nav-button"
                    maxWidth="Infinity"/>
            
            <Button fx:id="btnUtenti" text="👥 Gestione Utenti" 
                    onAction="#apriGestioneUtenti"
                    styleClass="nav-button"
                    maxWidth="Infinity"/>
            
            <Button fx:id="btnPrestiti" text="📋 Gestione Prestiti" 
                    onAction="#apriGestionePrestiti"
                    styleClass="nav-button"
                    maxWidth="Infinity"/>
            
            <Region VBox.vgrow="ALWAYS"/>
            
            <Separator/>
            
            <!-- Bottom Actions -->
            <Label text="ALTRO" styleClass="sidebar-section-label">
                <padding>
                    <Insets top="10.0" bottom="5.0" left="5.0"/>
                </padding>
            </Label>
            
            <Button text="ℹ Info" 
                    onAction="#mostraInformazioni"
                    styleClass="nav-button-secondary"
                    maxWidth="Infinity"/>
            
            <Button text="🚪 Esci" 
                    onAction="#esci"
                    styleClass="nav-button-secondary"
                    maxWidth="Infinity"/>
        </VBox>
    </left>
    
    <!-- CENTER: Area centrale per caricare le view -->
    <!-- Il contenuto viene caricato dinamicamente dal MainController -->
    
    <!-- BOTTOM: Status Bar -->
    <bottom>
        <HBox styleClass="status-bar" spacing="20.0" alignment="CENTER_LEFT">
            <padding>
                <Insets top="8.0" right="15.0" bottom="8.0" left="15.0"/>
            </padding>
            <Label text="Sistema Gestione Biblioteca Universitaria - Pronto" styleClass="status-label"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="v1.0" styleClass="status-label"/>
        </HBox>
    </bottom>
    
</BorderPane>